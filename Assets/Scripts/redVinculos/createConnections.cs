using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class createConnections : MonoBehaviour
{
    //// *** Get data from Json *** //

    //public JSONReaderAnalitica json;

    //// *** General band *** //

    //private bool isConnectionReady;

    //// *** Connections  *** //

    //public GameObject connectionManager;

    //private RectTransform nodeZero;

    //private Transform[] children;

    //private Connection anotherScript;

    //public RectTransform canvasNodes;

    //private Transform[] childConnections;

    //private int index = 1;



    //// Update is called once per frame
    //void Update()
    //{

    //    if (!isConnectionReady)
    //    {

    //        if (this.GetComponent<createNodes>().isReady)
    //        {
    //            Destroy(connectionManager.GetComponent<Transform>().GetChild(0).gameObject);
    //            Destroy(connectionManager.GetComponent<Transform>().GetChild(1).gameObject);

    //            childConnections = connectionManager.GetComponentsInChildren<Transform>();


    //            foreach (RectTransform child in canvasNodes)
    //            {
    //                if (child.name == "Node0")
    //                {
    //                    nodeZero = child;
    //                }
    //                else
    //                {
    //                    anotherScript = childConnections[index].GetComponent<Connection>();
    //                    anotherScript.SetTargets(nodeZero, child);

    //                    index++;
    //                }

    //            }

    //            isConnectionReady = true;
    //        }
    //    }

    //}
}
